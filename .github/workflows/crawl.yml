name: 每日自动抓取 AI 设计资讯

on:
  # 每天北京时间 0:00（UTC 16:00）自动触发
  schedule:
    - cron: '0 16 * * *'  # UTC 16:00 = 北京时间 0:00
  # 允许手动触发
  workflow_dispatch:

jobs:
  crawl:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: scripts/package.json

      - name: 安装依赖
        working-directory: ./scripts
        run: |
          npm ci

      - name: 运行抓取脚本
        working-directory: ./scripts
        run: |
          node crawler.js
        env:
          TZ: Asia/Shanghai

      - name: 检查是否有更改
        id: check-changes
        run: |
          git diff --exit-code data/today.json || echo "changed=true" >> $GITHUB_OUTPUT

      - name: 提交更改
        if: steps.check-changes.outputs.changed == 'true'
        run: |
          git config --local user.email "liu.shimeng309@gmail.com"
          git config --local user.name "liushimeng309-ai"
          git add data/today.json
          git commit -m "🔄 自动更新: $(date '+%Y-%m-%d') 的 AI 设计资讯" || exit 0

      - name: 推送更改
        if: steps.check-changes.outputs.changed == 'true'
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          force: false
